<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Battery Overview Card -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardElevation="6dp"
            app:cardCornerRadius="16dp"
            android:layout_marginBottom="16dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp"
                android:orientation="vertical">

                <TextView
                    android:text="Battery Overview"
                    android:textStyle="bold"
                    android:textSize="20sp"/>

                <TextView android:id="@+id/cycleCount"/>
                <TextView android:id="@+id/healthPercent"/>
                <TextView android:id="@+id/designCapacity"/>

            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Live Charging Card -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardElevation="6dp"
            app:cardCornerRadius="16dp"
            android:layout_marginBottom="16dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp"
                android:orientation="vertical">

                <TextView
                    android:text="Live Charging Data"
                    android:textStyle="bold"
                    android:textSize="20sp"/>

                <TextView android:id="@+id/voltage"/>
                <TextView android:id="@+id/current"/>
                <TextView android:id="@+id/power"/>
                <TextView android:id="@+id/battTemp"/>
                <TextView android:id="@+id/cpuTemp"/>
                <TextView android:id="@+id/directCurrent"/>
                <TextView android:id="@+id/chargingStep"/>
                <TextView android:id="@+id/timeToFull"/>

            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Graph -->
        <com.thomas.chargingoverlay.PowerGraphView
            android:id="@+id/powerGraph"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_marginBottom="16dp"/>

        <!-- Buttons -->
        <Button
            android:id="@+id/startBtn"
            android:text="START MONITOR"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <Button
            android:id="@+id/stopBtn"
            android:text="STOP MONITOR"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"/>

    </LinearLayout>
</ScrollView>